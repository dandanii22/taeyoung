const get = (target) => document.querySelector(target);
const getAll = (target) => document.querySelectorAll(target);

const login = () => {
  let btn = document.querySelectorAll(".loginbtn");
  let id = document.querySelector("idvalue");
  let pw = document.querySelector("pwvalue");

  btn.addEve;
};

(() => {
  login();
})();
